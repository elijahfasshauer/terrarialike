/// @description Draw grid

//Block width

//Vertical lines
/*
h=0;
for(h=0; h<width; h++) 
{
	draw_line(h*b_w, 0, h*b_w, room_height)
}

//Horizontal lines
w = 0;
for(w = 0; w<height; w++)
{
	draw_line(0, w*b_w, room_width, w*b_w);
}